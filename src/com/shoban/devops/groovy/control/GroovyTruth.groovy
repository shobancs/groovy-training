package com.shoban.devops.groovy.control

map = [:]
assert !map

list = ["Ubuntu", "Android"]
assert list
assert !0
assert 1
assert -1
assert !""
assert "Hello"
def test = null
assert !test

def name = "John"
assert name == "Peter"